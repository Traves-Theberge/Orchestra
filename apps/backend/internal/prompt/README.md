# Prompt Builder

Workflow prompt loading and template rendering for issue runs.

## Files
- `builder.go`: Loads workflow prompt templates and injects issue/attempt context.
- `builder_test.go`: Covers rendering success and unresolved/invalid template failures.
