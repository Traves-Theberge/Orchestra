# Runtime Identity

Runtime-level host identity and token policy helpers.

## Files
- `identity.go`: Defines runtime identity constants and host/token requirement checks.
- `identity_test.go`: Confirms loopback vs non-loopback token-requirement behavior.
