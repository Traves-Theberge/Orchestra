# orchestrad Daemon Entrypoint

This package starts the long-running backend daemon process.

## Files
- `main.go`: Initializes logging and runs the backend service lifecycle with fatal exit on unrecoverable startup errors.
