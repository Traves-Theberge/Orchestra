# Command Entrypoints

This directory groups executable entrypoints for the backend.

## Subdirectories
- `orchestra/`: CLI utility for checks and service startup commands.
- `orchestrad/`: Daemon startup entrypoint.
